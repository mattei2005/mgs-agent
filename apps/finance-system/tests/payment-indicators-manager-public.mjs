import {chromium} from '@playwright/test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';
const base='https://dash.mgsdigitalcorp.com',chunks=[];for await(const c of process.stdin)chunks.push(c);const credential=JSON.parse(Buffer.concat(chunks).toString());let browser;try{browser=await chromium.launch({headless:true,executablePath:'/root/.cache/ms-playwright/chromium-1234/chrome-linux64/chrome',args:['--no-sandbox']});const ctx=await browser.newContext({viewport:{width:390,height:844}}),page=await ctx.newPage();await page.goto(base);await page.waitForURL('**/login');await page.locator('#username').fill(credential.username);await page.locator('#password').fill(credential.password);await page.locator('button[type=submit]').click();await page.waitForURL('**/operations?view=manager');const me=await(await ctx.request.get(base+'/api/auth/me')).json();assert.equal(me.role,'manager');const ledger=await(await ctx.request.get(base+'/api/finance/ledger?period=2026-09')).json();assert.equal(ledger.indicators,null);await page.goto(base+'/operations?view=payments&period=2026-09');await page.locator('#party').waitFor();assert.equal(await page.locator('[data-payment-indicators]').count(),0);const logout=await ctx.request.post(base+'/api/auth/logout',{headers:{Origin:base,'X-CSRF-Token':me.csrf},data:{}});assert.equal(logout.status(),200);const out={pass:true,role:'manager',company_indicators_api:null,company_indicators_ui:0,manager_scope_preserved:true,financial_writes:0};await fs.writeFile('/root/mgs-agent/work/finance-payments-indicators-1547706561210753114/manager-privacy-readback.json',JSON.stringify(out,null,2));console.log(JSON.stringify(out));}finally{await browser?.close();}
