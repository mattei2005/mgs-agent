# Ops Summary — 02 de Maio 2026 (Parte 2) — P0 Fixes

**Período:** 02/05/2026 — sessão tarde  
**Responsável:** Rodolfo Mattei (CEO)  
**Registrado por:** Zeus (02/05/2026)

---

## 🔴 P0-1 — generate-rec-api.py Credential Parsing Frágil

### Problema

```python
# ANTES (linha 51) — frágil:
os.environ.get("ANTHROPIC_API_KEY") or open(".env").read()
# Se env var ausente → lia .env INTEIRO (19KB) como string
# Validação só na primeira chamada à Anthropic → erro 401 obscuro
```

### Fix Aplicado

| Item | Detalhe |
|---|---|
| Helper | `_load_anthropic_api_key()` com 2 estratégias em ordem |
| Validação | `startswith("sk-ant-")` em ambas as estratégias |
| File handling | `with open()` (context manager — fecha file handle) |
| Parsing | Itera linha por linha do `.env` |
| Falha | `raise RuntimeError` com mensagem clara se nenhuma estratégia funciona |

### Resultado

- ✅ API fail-fast **no startup** se credencial inválida (não na primeira chamada)
- ✅ Service restartado, health check OK (3 checks: `api` / `cache_db` / `templates`)
- Backup: `/tmp/generate-rec-api-20260502_110632.bak`

---

## 🔴 P0-2 — auto-commit-watcher + .gitignore (Repo Cleanup)

### Problema

| Sintoma | Detalhe |
|---|---|
| Volume de commits | 334 em 7 dias (~48/dia) |
| Lixo commitado | ~12% eram binários (`.db`, state files, `.pyc`) |
| Causa | `git add -A` no watcher capturava tudo, ignorando `.gitignore` |
| Escopo | 98 arquivos lixo trackados ativamente no repo |

### Fix em 4 Partes

**Parte 1 — Salvar patch discord_tool no local correto**
- `discord_tool-patch.diff` movido de backup para `patches/hermes/discord_tool/`
- `README.md` explicativo adicionado para o próximo upgrade do Hermes

**Parte 2 — Deletar do disco (arquivos redundantes)**

| Arquivo/Dir | Tamanho | Justificativa |
|---|---|---|
| `backups/pre-hermes-upgrade-20260429_104523/` | 20 MB | Coberto por snapshot Hetzner 381460955 |
| `backups/pre-hermes-upgrade-20260429_104523.tar.gz` | 2.9 MB | Idem |
| `api/__pycache__/` | — | Python regenera automaticamente |

Validado via `diff`: nenhum arquivo era único do backup antes de deletar.

**Parte 3 — .gitignore robusto + untrack**

- `.gitignore` expandido: 63 linhas — bloqueia `*.db`, `*.bak*`, state files, `__pycache__`, `backups/`, lockfiles, caches
- 11 arquivos untrackados do repo (mantidos no disco):
  - `api/usage.db`
  - `data/article-tracker.db`
  - `data/card-cache.db`
  - `data/*-state.json`
  - + outros
- `auto-commit-watcher.sh`: `git add -A` → `git add .` (respeita `.gitignore`)

**Parte 4 — Cleanup phantom records**

Race condition do watcher havia capturado só deleções num commit anterior, deixando referências fantasma no index. Limpeza:

```bash
git ls-files | while read f; do [[ ! -e "$f" ]] && git rm --cached "$f"; done
```

Resultado: 0 `backups/`, 0 `__pycache__`, 0 `*.db`, 0 `state.json` trackados.

### Validação Final

| Check | Resultado |
|---|---|
| Test funcional | `test-ignore.db` criado no disco → `.gitignore` bloqueou ✅ |
| Watcher | Active (PID 149477) ✅ |
| Working dir | 48 MB → **26 MB** (−46%) |
| `.git/` | 18 MB (mantido) |

---

## 📊 Status Geral da Auditoria

| Prioridade | Resolvidos | Total | Status |
|---|---|---|---|
| **P0 críticos** | **4** | **4** | ✅ **100% concluído** |
| P1 altos | 2 | 14 | 🟡 Em andamento |
| P2 docs | 0 | 22 | ⏳ Pendente |
| P3 lixo | 0 | 12 | ⏳ Pendente |

**Total fixes nesta sessão (02/05):** 8 aplicados, zero regressões.

**Próximo:** Onda 1 dos P1 — datetime deprecated restantes + dead code (~30 min estimado).

---

### Backups de Segurança em /tmp/

- `gitignore-20260502_122629.bak`
- `auto-commit-watcher-20260502_122629.bak`
- `generate-rec-api-20260502_110632.bak`
