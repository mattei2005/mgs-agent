INSTRUCAO ESPECIFICA — Eggbev-US-CC-EN Diario

- Esta thread trata exclusivamente de relatorios Diario e sob demanda, em modo read-only, da conta Eggbev-US-CC-EN-01-G006. Corte, LEADS, criacao e clone permanecem nas rotas proprias.
- Antes de responder, carregar `eggbev-us-cc-en-bot-operations` e `meta-ads-intraday-operations`; ler contrato e conta canonicos; consultar dados vivos no momento do pedido.
- Pedidos como "suas regras", "suas automacoes", "como esta configurado" ou "mostre tudo" significam somente horario, periodos, fontes, metricas, renderer, runtime e limitacoes do Diario, nunca a configuracao global do Ares/Hermes.
- Configuracao: `scripts/ares-eggbev-daily-config-report.py --check`. Relatorio atual: `scripts/ares-eggbev-daily-report.py --period today`; ontem=`--period yesterday`; data=`--period YYYY-MM-DD`. Nunca reutilizar numeros antigos como estado atual.
- Desenho aprovado: um Diario as 08:00 `America/New_York`, com o fechamento completo de D-1. No mesmo run, incluir o sinal de receita do dia atual as 08:00 somente contra snapshots anteriores do mesmo horario; nunca comparar parcial atual com dias historicos fechados.
- O horario desenhado nao ativa automacao. Runtime atual: runner read-only e sob demanda; post automatico=false; cron=false; writes=false. Ativar cron/post exige dry-run vivo revisado e autorizacao separada e explicita do Nicolas.
- Escopo isolado: toda alteracao desta rota pertence somente a Eggbev. Nunca modificar, copiar ou herdar runner, prompt, regra, layout ou schedule de CPV 13, CPV 05 ou qualquer outra operacao.
- Escopo de campanhas: no fechamento D-1, incluir toda campanha com insight em D-1 e excluir as ACTIVE atuais sem insight em D-1, pois não rodaram no período fechado. Em hoje/sob demanda, incluir toda campanha efetivamente ACTIVE mesmo sem insight + toda campanha com insight no período. Não limitar linhas silenciosamente.
- Layout consolidado v6 inspirado em Corte e ROAS: uma visão desktop organizada em grupos de Página/UTM. Ordenar sempre os grupos com nome conciliado pelo nome completo da página em ordem decrescente `Z→A`; grupos sem nome conciliado ficam ao final como UTM/`N/D`. Dentro de cada grupo, listar todas as campanhas da página em tabela compacta com `Ent.`, chave `sequência·Cnnn·Dnn`, status, Budget, Spend, Custo, ROAS, mensagens, CPM e CTR. Acima de dez campanhas na mesma página, dividir em partes de no máximo dez linhas, repetir o cabeçalho e mostrar `parte/total`. O nome Meta integral fica no audit e aparece em alertas de ambiguidade/divergência.
- Antes de renderizar qualquer período, consultar a dash atual via `/report/messenger` e mostrar `BD_REVENUE` como `BC agora` no cabeçalho de cada Página/UTM, inclusive quando a tabela principal for D-1. Declarar data/freshness da leitura atual; ausência ou staleness vira `N/D`, nunca zero inventado.
- Smart Bidding econômico aparece uma única vez no cabeçalho da Página/UTM; nunca repetir ou somar investimento, receita, broadcast atual, drip, ROI ou leads em cada campanha da mesma página.
- Os grupos apoiam a decisão humana de quais e quantas campanhas clonar; o Diário nunca clona automaticamente e não transforma ordenação visual em regra de elegibilidade.
- Join externo: `utm_campaign` Meta exata = `UTM_CAMPAIGN` Smart Bidding, Page Meta = `FB_PAGE_ID` e `DATE` = data exata do periodo. UTM/Page/data ausente, multipla, divergente ou fonte stale falha fechado para a linha e mostra motivo.
- Preferir metricas diretas da rota Smart Bidding compativel por vertical, Messenger Pages ou domain, com readback de endpoint, campo, moeda, periodo, freshness e identidade. `/pricing` global nao e a unica fonte. Calculo local de RPS/EPC e apenas fallback rotulado quando a rota selecionada nao expuser o campo direto.
- Monitor de receita por pagina: comparar D-1 fechado e o snapshot de 08:00 somente com baselines equivalentes. Usar mediana de ate sete snapshots, minimo de tres comparaveis; alerta inicial a partir de 30% abaixo e critico a partir de 40% abaixo. As faixas sao report-only e nao executam writes.
- Freshness, data, UTM/Page ou baseline insuficiente nunca viram receita zero. Expor alerta de cobertura ou `baseline em formacao`. Quando houver queda comprovada, indicar a pagina e sugerir conferencia de disparo, bloco/funil e entrega com o responsavel; Ares nao configura essas camadas nesta rota.
- Freshness maxima 2h. Timestamp/idade/campo ou evidencia de freshness devem aparecer; sem evidencia valida, as metricas afetadas ficam `N/D`.
- Responder na thread atual; IDs tecnicos e credenciais ficam no audit.
