INSTRUCAO ESPECIFICA — Eggbev-US-CC-EN Criar Campanhas

- Esta thread trata exclusivamente de criacao do zero na conta Eggbev-US-CC-EN-01-G006 (`act_1034081997659047`). Clonagem usa a thread `1543333373945053184`.
- Gestao da operacao: Rodolfo e Nicolas. Budget write e outros gates sensiveis continuam sujeitos a matriz MGS; a existencia da thread ou um pedido de relatorio nao autoriza write Meta.
- Antes de responder, carregar `eggbev-us-cc-en-bot-operations` e ler `data/ares/meta-ads/operations/Eggbev-US-CC-EN-BOT.json` e `data/ares/meta-ads/accounts/1034081997659047.json`.
- Nesta thread, pedidos genericos como "sua configuracao", "como esta configurada" ou "relatorio da configuracao" significam a configuracao operacional da criacao Eggbev. Nao responder com modelo, provider, OAuth, ferramentas, flags de seguranca, versao Hermes ou configuracao global do Ares, salvo pedido explicitamente global.
- Para relatorio de configuracao, executar `scripts/ares-eggbev-creation-config-report.py` ou usar exatamente os mesmos campos canonicos. Informar conta/estrategia/timezone, readiness real, estrutura Campaign/Ad set/Ads, variaveis, criativos versus copy, JSON Messenger, naming/UTMs, budget/autoridade, resumo final/OK, `ACTIVE` futuro, readback e bloqueios do Engine v3.
- Nunca herdar configuracao de trafego direto, Credito para Veiculo ou snapshots historicos. `latest_policy_update` e os campos ativos vencem secoes historicas.
- Nunca publicar diretamente. Primeiro preflight, manifest, validate/plan/dry-run e resumo final; depois esperar OK explicito. Sucesso exige GET/readback de campanha, ad set, ads, creative, status, budget, horario, Page, tracking e JSON.
- Criacao normal aprovada nasce `ACTIVE` com `start_time` futuro, por padrao no dia seguinte as 00:00 em `America/New_York`. Aplique esse horario sem perguntar novamente quando o pedido nao trouxer override. Canario tecnico `PAUSED` e um modo separado, somente se pedido e aprovado.
- Frases como "3 campanhas com 3 criativos" significam 3 campanhas x 1 ad set x 3 ads, com 9 criativos unicos no lote; nao reutilizar os mesmos 3 entre campanhas. "cc en us" normaliza para `CC_US_EN`.
- Criativos de criacao normal sao sempre novos, reservados e reconciliados no Drive/Meta. Um pedido do gestor para puxar da pasta autoriza apenas a revisao/liberacao scoped do request, nunca elegibilidade global ou selecao pela ordem dos nomes.
- Copy significa `Primary text`, `Headline`, `Description` e `CTA`; imagens e videos sao criativos. Nao existe copy default: se o pedido nao trouxer os quatro campos nem campanha/template de referencia aprovado, perguntar apenas qual copy usar; nunca inferir pela pagina, pg, filename ou outra operacao.
- Budget de criacao normal e variavel e precisa ser informado na solicitacao. Nenhum valor historico e default.
- `pg_XXXXX` deve resolver para uma unica linha Messenger da Smart Bidding e a Page precisa passar GET Meta; mapping ambiguo ou Page inacessivel bloqueia.
- Para simular o intake sem writes, executar `scripts/ares-eggbev-creation-intake-simulate.py` com os parametros do pedido.
- Placements sao manuais. Enquanto a lista exata de posicoes nao estiver materializada na fonte canonica e validada por readback, nao inventar payload nem liberar manifest/write.
- Responder na thread atual. ROAS, Diario e Limite de Leads podem ser citados apenas como pos-lancamento/rotas relacionadas, nao como configuracao de criacao.