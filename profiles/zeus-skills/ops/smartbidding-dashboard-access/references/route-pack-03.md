### Page tab

`Messenger > Page` shows pages and installed template fields. This is also where page-level broadcast schedules live: the schedule is the row's `BROADCAST_TIME`/Broadcast tab data tied to a selected `BROADCAST_TEMPLATE_NAME`, not necessarily a property of the Broadcast Template message body itself.

#### Linkar template em uma página

Rodolfo correction 2026-07-07: para vincular um Broadcast Template a uma página específica, ir em `Accounts > Messenger > Page`, filtrar pelo **FB_PAGE_ID grande**, validar também o **PAGE_ID pequeno** e o **PAGE_NAME**, selecionar a row, clicar em `Editar` no canto superior direito, abrir a aba `Broadcast`, escolher o template no primeiro campo `Message Template`, e salvar. Via API, a operação equivalente deve alterar somente os campos de template da row alvo (`BROADCAST_TEMPLATE_ID`/`BROADCAST_TEMPLATE_NAME` quando presentes), preservando os demais campos; validar por readback da mesma row (`FB_PAGE_ID`, `PAGE_ID`, `PAGE_NAME`, `BROADCAST_TEMPLATE_NAME`).

#### Corrigir Messenger User da página

Rodolfo correction 2026-07-07: quando uma Sheet de auditoria indicar divergência entre usuário do Bot/DTR e usuário na Dash, e Rodolfo confirmar que a coluna A é o usuário correto do bot e a coluna H é o usuário errado/atual da Dash, corrigir `Messenger User` da página em `Accounts > Messenger > Page`. Validar a row por `FB_PAGE_ID` grande + `PAGE_ID` pequeno + `PAGE_NAME`, editar a página, trocar o campo `Messenger User` para o login correto da coluna A, salvar e fazer readback. Via API, descobrir o `MESSENGER_USER_ID` correspondente ao login correto a partir de outra row/endpoint de users antes de postar; nunca trocar só o texto `USER_LOGIN` sem ID validado.

Correção operacional 2026-07-08: às vezes a Page row já aponta para o `MESSENGER_USER_ID` certo, mas o registro em `Accounts > Messenger > User` (`GET /users/Messenger`) ainda tem `LOGIN` antigo. Nesse caso, tentar escrever `USER_LOGIN` na Page row pode dar HTTP 500 e não resolve a coluna `LOGIN`. O fluxo correto é editar o Messenger User record em `/users/Messenger`: backup do user record, `POST /users/Messenger` com payload mínimo `ID`, `ACCOUNT_ID`, `PUBLISHER_ID`, `NAME`, `LOGIN` — sem campos extras como `COMPANY`, `URL`, `ACTIVE`, `ID_1` porque isso gerou HTTP 500. Depois validar por `/users/Messenger` e por `/campaigns/Messenger` da Page alvo: `LOGIN` deve virar o login correto, `MESSENGER_USER_ID` preservado, e `PAGE_ID`, `FB_PAGE_ID`, `UTM_CAMPAIGN`, `STATUS` preservados.

#### Cadastrar página Bot/DTR ausente na Smart Bidding

Rodolfo video correction 2026-07-07: para cadastrar páginas da aba `gid=907050576`, ir em `Accounts > Messenger > Page`, clicar `New Page`, preencher a aba `Page` com `Messenger User`, `FB Page ID`, `Page ID`, `Page Name`, `Country`, `Vertical`, `Source`, `UTM Campaign`, `Status`, `Notes`; depois abrir a aba `Broadcast`, escolher o `Message Template` referente ao site/login (não decidir por nome da página), preencher `Current Message ID`, `Message ID`, e `Scheduled Times`, então salvar. Via API, operação equivalente é `POST https://api.jbfdigital.com.br/campaigns/Messenger` com `MESSENGER_USER_ID`, `PAGE_ID`, `FB_PAGE_ID`, `PAGE_NAME`, `UTM_CAMPAIGN`, `STATUS`, `SOURCE`, `VERTICAL`, `COUNTRY`, `NOTES`, `BROADCAST_TEMPLATE_ID`, `BROADCAST_TIME`, `BROADCAST_CURRENT_MESSAGE_ID`, `BROADCAST_MESSAGE_ID`. Capturar headers reais do SPA; direct unauth API retorna 401. Validar por readback no full Page scope usando `FB_PAGE_ID` e `PAGE_ID`.

Correção de pré-cadastro 2026-09-03: filtro por `PROFILE_NAME` retornando zero não prova que a página está ausente. Antes de `New Page`, buscar cada candidata no escopo full live por `FB_PAGE_ID` grande e `PAGE_ID` pequeno, sem filtro de perfil. Se a row já existir com `PROFILE_NAME=null`, ela não deve ser recriada: isso é falha/ausência da associação do Messenger User/perfil, não página ausente. Também validar o registro exato em `/users/Messenger`; `ACTIVE=false` é um bloqueio de User, e reativá-lo pode afetar todas as páginas do login, portanto não cabe em um canário autorizado apenas para uma página. Exemplo validado: as 23 páginas DTR de Ione Silva já existiam 23/23 no SB, todas com `PROFILE_NAME=null`, sob o login `disparoszuout@gmail.com`; tentar cadastrar uma nova row criaria duplicidade ou retornaria 409. O próximo passo correto exige autorização separada para reparar a associação/User, não um POST de criação de Page.

Correções de enum/dropdown do modal: campos da Sheet são labels humanos, mas a API precisa receber os valores internos usados pelas rows existentes, senão o GET retorna texto mas o modal fica com dropdown vazio. Mapear `Status READY -> Ready`, `Country United States -> US`, `Vertical Credit card -> CC`, `Source Facebook -> FACEBOOK`. Para a aba específica das ~150 páginas (`gid=907050576`, resolução da aba de páginas pendentes), `BROADCAST_TIME`/Scheduled Times deve ficar **somente `08:00`**, não copiar todos os horários de uma row existente; Rodolfo corrigiu isso explicitamente após Bruna/Greta. Se o Messenger User exato da Sheet não existir mas existir variante óbvia do mesmo site/vertical sem sufixo extra (ex.: `disparosfinanceadxcafr@gmail.com` ausente, `disparosfinanceadxca@gmail.com` existente), Rodolfo autorizou usar a variante existente para colocar a página na Dash; o gestor troca depois. Se uma página foi removida do DTR e a API responder `409 This FB_PAGE_ID already exists` mas a row não aparecer no escopo full SB, tratar como bloqueio/registro oculto e não insistir. A validação final não pode ser só direct `GET /campaigns/Messenger/{ID}`; precisa confirmar que os enums internos aparecem no readback full-scope (`COUNTRY=US`, `VERTICAL=CC`, `SOURCE=FACEBOOK`, `STATUS=Ready`) e que `BROADCAST_TIME == ["08:00"]` para essa aba.

Canary validation correction: `Madelyn Riley`, `PG 2989`, `FB 507130289143976` initially posted `STATUS=READY`, which made the table show a gray badge but left the edit modal `Status` dropdown unselected. Correct SB enum is proper-case `STATUS=Ready`; after reposting the exact row with `Ready`, readback returned `STATUS=Ready`. For future creates, map Sheet `READY` to API/UI `Ready`, not uppercase `READY`. Bruna Babdinto correction 2026-07-07: posting `COUNTRY="United States"`, `VERTICAL="Credit card"`, `SOURCE="Facebook"` also left the Page-tab dropdowns blank; reposting `US`/`CC`/`FACEBOOK` plus copied `BROADCAST_TIME` fixed the row by readback.

Intent correction: if Rodolfo says `cadastra essa` while giving login/email + large `FB Page ID` + small `Page ID`/PG + page name, especially with the registration Sheet link, interpret it as this SB Page registration flow — not as agent/user authorization. Locate the exact Sheet row and execute all fields. For readback and final DTR↔SB audits, accept `LOGIN` as the login field when `USER_LOGIN` is null on newly created rows; otherwise bulk audits can falsely classify freshly-created SB rows as `NO_SB_MATCH`. Use direct `GET /campaigns/Messenger/{ID}` if the full list has not refreshed yet. Detailed session notes: `references/sb-page-registration-from-sheet-intent-and-readback-2026-07-07.md`.

For temporary Facebook/Messenger send restrictions discovered in DigitalTRChat campaign reports, edit the Page row and use the **Broadcast** tab's `Restricted Until` field. Latest Rodolfo-approved rule after the July 2026 audit: if the latest current DigitalTRChat report shows pure `#2022`, set Page `Status = Broadcast` and `Restricted Until = X + 1 calendar day`; later, when the restriction expires and the page should return to operation, clear `Restricted Until`, save, and restore `Status = Broadcast`. Keeping `Status = Broadcast` with only `Restricted Until` was an earlier approach and should only be used if Rodolfo explicitly asks for that behavior.

