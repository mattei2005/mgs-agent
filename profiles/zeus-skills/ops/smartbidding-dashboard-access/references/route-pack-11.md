## Update 2026-07-02 — restricted pages daily monitor

Zeus criou monitor dedicado para páginas Messenger restritas: `/root/mgs-agent/scripts/monitor-sb-restricted-pages.sh` → Python `/root/mgs-agent/scripts/monitor-sb-restricted-pages.py`, state `/root/mgs-agent/data/sb-restricted-pages-monitor.json`, log `/root/mgs-agent/logs/monitor-sb-restricted-pages.log`, cron `0 8,16 * * *` com `flock`, canal Discord `1522442220903337984`. O monitor lê live `Accounts > Messenger > Page` via `/campaigns/Messenger`, escopo publishers ativos `digital-trust + digital-trust-2`, filtra `STATUS=Broadcast` + `RESTRICTED_UNTIL >= hoje`, e envia uma mensagem ao Discord após toda varredura `apply` concluída com sucesso: alerta detalhado quando houver páginas novas aplicadas na SB e confirmação curta `Nenhuma página restrita nova até o momento, comparado com a última varredura concluída` quando não houver novas. Nunca enviar confirmação de zero se a varredura tiver erro operacional, pois seria um falso all-clear. Modelo histórico de 2026-07-02: 2 mensagens em code block sem linguagem — bloco principal com resumo/por data/novas e bloco separado para legenda de erros. **Esse layout foi substituído pelo formato final aprovado em 2026-07-14 abaixo, que inclui o link clicável da Google Sheet somente na primeira mensagem.** Na seção `POR DATA DE SAÍDA`, ordenar por data crescente (menor → maior), nunca por volume/quantidade de páginas; Rodolfo quer leitura cronológica do que sai primeiro. Regra de segurança de layout: enquanto o cron `monitor-sb-restricted-pages.sh` for SB-only, o alerta não pode exibir `hora DTR`, `Código erro` ou `DTR pendente` como se fossem dados lidos; deve rotular a origem como `SB-only; DTR não lido` ou ocultar colunas DTR até o checker do Bot/DigitalTRChat estar acoplado e validado. DTR enrichment/update implementation status: detector `/root/mgs-agent/scripts/dtr-detect-restricted-pages.py` can log into DigitalTRChat and extract `#2022` + release date/time from recent Completed reports; SB updater `/root/mgs-agent/scripts/sb-set-restricted-until.py` remains a manual single-page dry-run/apply utility. **Do not enable production DTR→SB cron from all 1Password `Digitaltrchat - Disparos*` items.** Rodolfo corrected the scope: active bot users must come from the live `Migração 22/06` sheet, then the detector must log into each active bot user, iterate every top-bar segurador/account, enumerate all pages, inspect only the latest Completed report per page, cross-check live SB, skip `On-hold`/`Blocked`/active `RESTRICTED_UNTIL`, and only then apply current `#2022` (pure or mixed). Mixed `#2022 + other codes` must be saved for post-expiry review. Production cron is not ready until that full dry-run is validated. See `references/dtr-sb-restricted-sync-scope-correction-2026-07-03.md`. Execução inicial 2026-07-02 validou `3.237` rows, `48` publishers ativos e `209` páginas restritas ativas. Para layout, labels, colunas e legenda do report Sheet, seguir `references/sb-restricted-pages-sheet.md`.

**Formato final aprovado por Rodolfo em 2026-07-14:** o ciclo bem-sucedido publica mensagens separadas no canal, sem headings narrativos externos: (1) novas restrições aplicadas — ou confirmação de nenhuma nova — com o único link clicável da Google Sheet; nunca expor caminho local de XLSX/log; a seção `AÇÃO EXECUTADA` usa a frase humana `As páginas restritas acima foram atualizadas com a data de saída na Dash da Smart Bidding.`; (2) resumo operacional em bloco próprio, sem repetir o link da Sheet; (3) páginas que saíram da restrição em bloco próprio, somente quando houver saídas confirmadas por readback live. No resumo diário, `RESTRICTED_UNTIL` apenas preenchido não prova restrição ativa: reutilizar a definição canônica `active_restricted`, mantendo a data de hoje inclusiva e excluindo qualquer data anterior a hoje mesmo que o campo continue preenchido na SB. Assim, datas vencidas não reaparecem como se fossem novas restrições; elas pertencem somente ao fluxo confirmado de `PÁGINAS QUE SAÍRAM DA RESTRIÇÃO`. O teste de regressão deve cobrir ontem/hoje/amanhã e provar que somente hoje e futuro entram no resumo. No resumo, mostrar todos os nomes de sites, sem `+N sites`, reticências ou omissão. Ordenar sites alfabeticamente por família, mantendo o site-base e sua variante `finanzas` adjacentes — por exemplo `openzed` imediatamente seguido de `openzedfinanzas`, e `openzed.com` imediatamente seguido de `finanzas.openzed.com`. Se o limite de 2.000 caracteres do Discord exigir divisão, quebrar apenas entre linhas/data, repetir o cabeçalho e usar continuação explícita; nunca truncar a lista de sites. **Preferência de leitura aprovada em 2026-08-23:** usar um único emoji semântico apenas no início do título e também nas continuações, sem emojis dentro das tabelas: `🆕` novas aplicadas, `🔴` transições detectadas, `✅` varredura concluída, `📊` resumo operacional e `🟢` saídas da restrição.

**Sequência e validação obrigatórias do renderer:** antes do upsert da Sheet, comparar o dataset anterior com o dataset live desejado e preservar as linhas removidas. Uma linha removida da aba `Paginas` só entra em `PÁGINAS QUE SAÍRAM DA RESTRIÇÃO` quando a mesma página é encontrada no readback live da SB e `RESTRICTED_UNTIL` já não está ativo; não confundir mudança para `On-hold`, mudança de escopo ou ausência de match com saída de restrição. Depois: atualizar a Sheet, exigir readback com contagem e chaves únicas, renderizar na ordem primeira mensagem → resumo → saídas, e só então publicar. O teste do renderer deve usar fixture de alta cardinalidade e provar: todas as linhas renderizadas exatamente uma vez, cada mensagem com no máximo 2.000 caracteres, nenhum `+N sites`/reticência, todos os sites presentes, link da Sheet somente na primeira mensagem e cabeçalhos repetidos nas continuações. Em falha parcial de entrega, registrar os IDs já publicados antes de qualquer retry para não duplicar o primeiro bloco.

**Escritor concorrente SB — Iris:** a Iris pode aplicar ou renovar `RESTRICTED_UNTIL` entre os ciclos do Zeus. Portanto, o alerta detalhado nunca pode depender de o próprio `dtr-sb-page-health-sync.py` ter executado a escrita. `skip already restricted` continua válido apenas para evitar escrita/DTR scan redundante; não pode suprimir a detecção de uma nova transição na SB. Manter um detector independente de transições live, com baseline salvo somente após leitura completa, identidade estável da página e data de saída no estado para reconhecer extensão ou nova restrição da mesma página. Alertas originados apenas pela mudança observada na SB devem ser rotulados `Smart Bidding live; agente que escreveu não comprovado` até existir evidência direta da Iris/DTR. O resumo diário agregado não substitui esse detector e pode atrasar mudanças ocorridas depois do horário do resumo.

**Base ativa desde 2026-07-15, com regras de Sheet superseded em 2026-07-16:** `/root/mgs-agent/scripts/sb-restricted-transition-monitor.py`, estado `/root/mgs-agent/data/sb-restricted-transition-state.json`, log `/root/mgs-agent/logs/sb-restricted-transition-monitor.log`, cron `14,29,44,59 * * * *` com `flock` e Xvfb. Escopo: usuários ativos da planilha de migração, global ignore aplicado, `RESTRICTED_UNTIL >= hoje`; monitora `Broadcast` e também `Campaign`/outros status ativos, mantendo `On-hold` e `Blocked` fora da reação automática. Ciclos comparam o último snapshot concluído; falha de leitura, escrita da Sheet ou entrega não pode avançar o baseline. **Não aplicar mais** as instruções históricas de aba `Paginas`, Sheet somente Broadcast ou backup obrigatório desta ativação; a correção canônica de 2026-07-16 abaixo substitui integralmente esses pontos.

**Correção aprovada em 2026-07-16 — Sheet canônica reconstruída e sincronizada por transição:** a planilha `Paginas Restritas - Atualizacoes` usa `Resumo` imediatamente à esquerda de `Paginas Totais`. O escritor deve limpar e reconstruir integralmente todas as abas gerenciadas em cada atualização, nunca append incremental, e validar por readback contagem, chaves únicas e posição das abas. Para esta reconstrução canônica específica, Rodolfo dispensou explicitamente backup porque o conteúdo anterior estava incorreto; não generalizar essa exceção para outras planilhas. `Paginas Totais` e abas por site incluem toda restrição ativa alertável (`Broadcast`, `Campaign` e outros status), excluindo `On-hold` e `Blocked`; assim qualquer página que gere alerta de transição existe também na Sheet. O `Resumo` mostra `Broadcast restritas`, `Outras restritas ativas`, `On-hold ignoradas` e agrega `Data de Saída | Páginas | Sites` sobre todas as linhas incluídas. O resumo Discord permanece estritamente `Broadcast`. O cron de transições faz o rebuild da Sheet com lock compartilhado antes de publicar entrada/saída e antes de avançar o baseline. Uma saída só é alertada quando a página é encontrada no readback SB live com `RESTRICTED_UNTIL` inativo; ausência de match, mudança de escopo e mudança para `On-hold`/`Blocked` não são saída confirmada. Regressão obrigatória: Campaign incluída na Sheet/site/Resumo, On-hold/Blocked e data vencida excluídos, resumo Discord sem Campaign, zero duplicidade e replay idempotente.

**Histórico por página ativo desde 2026-08-27:** a Sheet canônica mantém `Histórico Restrições` imediatamente à direita de `Paginas Totais`, com uma linha por identidade estável `bot user + page id` (fallback FB Page ID). O estado durável fica em `history` dentro de `data/sb-restricted-transition-state.json`. A aba mostra identificação, sites, `entradas detectadas`, `saidas confirmadas`, `renovacoes`, `mudancas de status`, estado atual e os últimos timestamps. Regras de contagem: `nova` incrementa entrada; mudança de data/`renovada*` incrementa renovação; mudança de status incrementa status; saída incrementa somente após o readback live confirmar `RESTRICTED_UNTIL` inativo, preservando a proteção contra On-hold/Blocked/ausência de match. Cobertura histórica começa em `2026-07-15T14:38:01-04:00`; restrições herdadas já ativas nessa data não são inventadas como entradas. O backfill canônico combina apenas roots `apply` concluídos do log, `removed_page_rows` de readback da Sheet e lacunas de saída reconciliadas por mensagens Discord/backup exatos. Em toda execução do monitor de 15 minutos, atualizar os contadores em memória, reconstruir a Sheet com a nova aba, exigir readback de contagem/chaves/posição e só então salvar o baseline+history; falha não pode avançar o estado. O writer DTR sem `history_rows` deve preservar a aba e nunca tratá-la como site. Regressão mínima: contadores independentes por página, replay idempotente, 571 chaves únicas no cutover inicial, paridade state↔Sheet e posição `Resumo`→`Paginas Totais`→`Histórico Restrições`.

**Correção de enriquecimento DTR preservado em 2026-07-30:** alguns rows válidos da SB retornam `LOGIN`, mas `PROFILE_NAME=null`; nesses casos, recuperar o segurador do segmento estruturado `PERFIL SEGURADOR - <nome>` em `NOTES`. Mais importante: rebuilds SB-only do monitor de transições não podem substituir campos DTR já verificados. SB continua canônica para presença da linha, `STATUS` e `RESTRICTED_UNTIL`; DTR continua canônica para segurador e códigos do último `Completed`. O escritor preserva o enriquecimento existente pela identidade estável `bot user + page id` (fallback FB Page ID), e uma varredura DTR fresca sobrescreve esse enriquecimento. Validar por readback as linhas exatas; esse gate evita segurador vazio e código histórico de NOTES reaparecerem após o próximo rebuild de 15 minutos.

Rodolfo corrigiu o desenho final: para horário real de saída, o monitor deve virar `SB filter + DigitalTRChat checker + Sheet writer`. SB só dá data; DTR é a fonte real do erro e hora. Páginas já restritas na SB devem ser puladas no DTR até expirarem. O relatório Discord não deve ter rodapé e deve ter seções `Resumo`, `Por data/hora de saída`, `Novas restrições detectadas`, `Ignoradas nesta rodada`, separando `On-hold` e `Blocked`. O report completo deve ir para a Sheet, com título `Páginas Restritas — MGS`, categoria `Restrita`, bot user sem `@gmail.com`, e a coluna `Página` como hyperlink `https://facebook.com/{FB_PAGE_ID}`. Detalhes em `references/sb-restricted-pages-monitor-and-report-layout-2026-07-02.md`.

Correção de desenho aprovada por Rodolfo: o monitor final deve ser **SB filter + DTR checker**. SB filtra o pool operacional (`Accounts > Messenger > Page`, `STATUS=Broadcast`, `digital-trust + digital-trust-2`) e pula páginas já com `RESTRICTED_UNTIL` ativo; DigitalTRChat é a fonte real para detectar novas restrições e extrair a data/hora exata de saída abrindo a última mensagem/report de cada página checável em todos os usuários/seguradores. Páginas restritas só voltam ao DTR check depois que expiram/limpam. Layout Discord aprovado: seções `Resumo`, `Por data/hora de saída`, `Novas restrições detectadas`, `Ignoradas nesta rodada`; sem rodapé; remover `@gmail.com` do usuário bot; separar `On-hold` e `Blocked`; usar `Sem report DTR válido` em vez de “sem último report útil”. Sempre preferir resumo curto no Discord + Excel completo quando houver mudança/erro. Ver `references/sb-restricted-pages-monitor-layout-and-dtr-enrichment-2026-07-02.md`.
