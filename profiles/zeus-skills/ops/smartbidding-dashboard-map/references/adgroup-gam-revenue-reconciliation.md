# AdGroup versus GAM revenue — read-only reconciliation

Validated with Yolokfx September7/8 2026, authorization1547412068875894815. Canonical financial exception and full evidence: `/root/mgs-agent/docs/gam-revenue-claude-yolo-september-2026.md`.

- Access using smartbidding-dashboard-access headed Playwright/Xvfb and canonical 1Password identity. Open `/reports/adgroup`; capture the actual request to `POST https://api.jbfdigital.com.br/report/performance_per_campaigns`, not a guessed `/adgroup` endpoint. Keep captured Authorization in memory, never output it.
- Request fields: `initialDate`, `finalDate`, `publishers`, `currency`. Publisher for this fixture: `digital-trust_yolokfx`, proven by live dashboard. Explicit `currency: "CAD"`; null returned BRL-like values and is not a safe original-currency selector. Use local-safe instants, e.g.15:00Z, and verify actual response DATE values (SP timezone).
- Read gross `REVENUE`; preserve NET_REVENUE and REVENUE_ESTIMATED separately. Do not compare a revenue-share-discounted primary UI value with gross GAM.
- Resolve manager from ACCOUNT_NAME terminal G00X with exact CUSTOMER_ID retained. Validate campaign code extracted from UTM_ADGROUP against CAMPAIGN_NAME where present. Preserve leading zeros/literal campaign tokens. Missing ACCOUNT_NAME is unresolved, not a license to guess from fb01/fb02.
- Response is a bare list in this fixture. Verify PK_JBF_PERFORMANCE_PER_ADGROUP uniqueness and publisher/domain/date scope. The7/8 two-day response had exactly100 rows, but was NOT a pagination limit: independent one-day requests returned47+53 and exactly the same100 objects/PKs. Always investigate round-number counts before declaring complete.
- Domain comparison: `GET /report/performance_per_domain` with matching date parameters, repeated `publishers[]`, and currency CAD. This fixture produced Domain2603.49/2931.77 versus AdGroup2524.39/2906.31 and emailed GAM2605.5630324995379395/2929.678978045648383496. No equality and no proven universal reason for the differences; preserve snapshot provenance.
- Where GAM lacks medium but retains utm_campaign, an exact same-day campaign→account identity join can assign ORIGINAL GAM revenue without substituting dashboard amounts. Retain every original line and unmapped residual. Revenue with campaign '-' or a campaign whose SB account is blank remains unresolved. Never silently prorate it or write finance during a read-only analysis.
