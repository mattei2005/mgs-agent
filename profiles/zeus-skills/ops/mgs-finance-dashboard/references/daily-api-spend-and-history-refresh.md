# Daily API spend and fresh monthly history — authorized1546991137171181578

## Authority and supersession
Rodolfo authorized filling September1–7 in Dash and recurring morning collection, importing registered accounts and reporting accessible accounts absent from the registry without automatically registering them. In the same turn he corrected Zeus twice: the previously authorized January–July own-month-source decision means use refreshed original-tab values, not refuse updates merely because the earlier imported capture was retained. This supersedes the inactive/read-only-spend statements in earlier references and the interpretation that original historical capture values must remain active. Preserve prior versions for audit, not as an obstacle. No August rules back-propagation, no source Sheet writes, no autonomous account allocation or ad/campaign write.

## Live history refresh
- `history_refresh.py` captures40 original monthly/manager tabs (January–July) using the canonical SA helper. It does not read Caixa. Normalize safe values/errors using the prior import metadata, preserve labels and original currencies; do not hard-code financial totals from conversation.
- `history-source-import.mjs` and `history-source-cli.mjs` append versioned history payloads to existing scenario storage and update the `master-history-source` pointer; original40 immutable finance_history rows remain audit evidence.
- `history.mjs` resolves the active version with period/book validation; missing/corrupt active pointers fail closed. No arbitrary fallback to an old value when an active version exists.
- Published with `deploy/history-source-release.py`, PostgreSQL staging first, original DB/code backup, version import and exact readback. Fresh MarchG138 was49687.428775020395USD before and49701.215616746515USD after; UI49.701,22USD. Do not generalize G137 as the half: G137 is the total; G138 is the half. Other source formulas can still change after capture.
- Historical pages remain non-editable in the UI; this authorized source refresh is separate from daily September+ media-spend collection. Future source refreshes need the same authorized scope, not a silent current-rules recalculation.

## Live spend pipeline
- `finance_spend_sources.py`: read-only exact-ID discovery from the configured MGS Meta Business and Google MCC. Collect registered accounts only; reconcile daily spend with the same-period aggregate. Preserve Meta currency and timezone; Google micros retain6decimal precision. Pagination, identity, timezone and currency are checked. Errors are not zeros. No secrets printed; canonical existing worker/client and SA only.
- `media-spend.mjs`: strict period/account/date validation, exact micro-unit sums, account+date keyed upsert, no duplication on replay. Prefer a single explicitly linked original input over derived aliases. More than one eligible input, missing link, incompatible currency or a divergent manual edit becomes an exception; do not guess allocation.
- Account/day facts remain visible even if not yet assignable to site P&L. Only uniquely mapped amounts reach the financial workspace. Three positive-spend exceptions at initial run: Google2780300411 Gamingadx-US-01, Google5172498094 Mattei1 (no site links); Meta3188620887977617 Infinitynexx-MX-CC-ES-01 (two original spend fields).
- `media-spend-cli.mjs`: PostgreSQL staging/canary, dry-run integrity, real writes with before-backup and exact readback, idempotent replay. Storage and audit are atomic with revision checks. `public/media-spend.js` is mounted in existing Contas view; list82 registered account/day details and all268 absent-from-registry accounts without auto-cadastro. Counts are runtime observations, not future fixed allowlists.
- Initial September1–7:82 registered accounts,574 account/day rows; USD111206.16 and BRL26938.430287. Site P&L received USD107466.07; USD3740.09 and BRL26938.430287 retained in the visible unassigned ledger.350 accessible accounts discovered;268 not registered (230Meta,38Google). No claim that unregistered accounts have spend: discovery was not spend collection for them.

## Automation
- Canonical contract: `/root/mgs-agent/data/finance-media-spend-contract.json`; state: `data/finance-media-spend-state.json`.
- Root crontab: `16 7 * * *`, America/New_York,25s stagger. Debian cron3.0pl1 uses host Eastern zone; do not rely on CRON_TZ. Job: `/usr/bin/python3 /root/mgs-agent/apps/finance-system/finance_media_spend_sync.py --scheduled` under a cron flock and900s bound.
- Each morning: month containing yesterday, day1 through yesterday; preserves actual account calendar dates and excludes today/future. Month rollover/DST tests cover the boundary. No automatic January–July reimport or changes before2026-09.
- Own sync lock plus quote-sync lock around financial publish. Stage/canary/full/replay use this same runner; zero field changes on replay is a real acceptance gate.
- Daily source errors are reported, prior values preserved for unconfirmed accounts. State tracks failures and halts scheduled work after5consecutive failed runs. Credential/permission problems escalate; do not rotate credentials or restart a gateway.
- Discord goes directly through canonical `discord-bot-post.py`, with explicit channel, bounded payload, no credentials or alternative env overrides, and exact message GET readback. Report this thread1545426987756298340 with owner mention only for exceptions/failures. Complete high-cardinality missing list is in Dash, not hundreds of Discord bullets. Test notification1547005040366190686 had exact readback.
- `deploy/media-spend-schedule-preflight.py`: run using a discovered interpreter containing croniter (system python3 lacks it); discover all active profile schedules, root and/etc cron, and systemd timers, expand8civil days. Initial accepted7:16 had zero operational collisions and unavoidable minutely baselines, with25s stagger. `deploy/media-spend-schedule-install.py` installs only after fresh proof and confirms all prior crontab entries intact; postcheck explicitly16after installation. Do not re-run install blindly: it intentionally rejects duplicates.

## Proof and rollback
Evidence: `apps/finance-system/private/spend-import-1546991137171181578/`, per-run `private/media-spend-runs/`, and production scoped before-backups in app private/media-spend-backups. Code/DB staging backups are retained by both release helpers; no destructive cleanup authorized. Main test suite:74Node tests passed;48Python tests passed in two24-test batches. A separate legacy original-logo pixel test is blocked by a missing transient WebP, not changed production logo; do not fake it by copying the current logo into the expected original fixture. Combined suites exceeded tool wall timeout, so split them and aggregate actual results. Real browser checked all574records,82accounts and268missing entries in1440px/390px with no errors; history separately checked7months×7views on both widths.

Do not declare the whole financial-system migration complete: this closes source refresh and daily spend, with explicit pending human mapping and no scheduled natural tick claimed before it occurs.
