# Manager presentation and financial total contrast

**Supersession:** `usability-google-finance-nicolas-jislaine.md` records the later approved engine monthly ROI (no longer unavailable when monthly source exists), dual-currency cards, actual Nicolas login and dark sidebar. This document is prior release evidence.

Authority: Rodolfo messages1546719646919434370 and1546720000134483970, thread1545426987756298340. Supersedes the prior capped manager daily tables and raw-decimal presentation, NOT engine, calculation rules, access or Nicolas-only pilot.

## Verified causes and presentation
- `manager-view.mjs` returns engine decimals as **strings**. `typeof v === 'number'` skipped money/ROI formatting. `managerNumber` in operations.js strictly accepts finite JS numbers and machine decimal strings, never coercing missing/blank/error/boolean/localized strings into zero. `managerValue` renders pt-BR two places and percentages, retains sign, escapes invalid text, and rounds only for display; state/precision never rewritten. Small displayed values normalize negative zero.
- Manager overview: three monetary cards with explicit USD/BRL, striped eight-domain summary and existing engine TOTAL; 7%/10% remain alternative references, not summed or changed rules.
- Daily blocks are grouped by suffix country or TOTAL. Default Total do domínio, country options preserve every original column/row. Country US/BR/GB is NOT currency: CAD origin remains explicitly identified, normalized results USD. Do not rename/calculate a country code as a currency.
- All actual calendar days rendered: August31, September30. `manager-block .table {max-height:none}`; document scrolls vertically, tables alone horizontally on small screens. Monetary month totals sum unrounded day values; no monthly ROI averaging/summing. Missing/error cells do not silently become zero; column totals containing actual nonnumeric errors remain unavailable. Sum rows are presentation-only and never written to database.
- Original TOTAL conflict: nested `.origin`/`.deduction`/`.result`/`.negative` spans repainted a dark total cell. Resolve children, not only td: `.finance-shell .subtotal td :is(span,strong,b,a){color:inherit!important;background:transparent!important;box-shadow:none!important}`. Covers company, site and other subtotal consumers while normal rows retain sign colors.

## Acceptance
- `tests/manager-layout.test.mjs`: RED before implementation; decimal strings, blank/error/zero, percent,31days, group coverage, state unchanged, total descendant CSS.
- `tests/manager-layout-browser.mjs before|stage|live`: protected owner credentials via stdin (same vault route as run-payments-public.py), no saved login state or financial POST. Stage intercepts only three static assets; API data remains real and read-only. Persist each month/group/row count, then aggregate;42 combinations across8blocks ×August/September at release, not a permanent universe assumption. Confirm page overflow and table vertical size numerically.
- Do not detect raw decimals with `\d+\.\d{3,}` alone: it flags valid Brazilian thousands, e.g.1.411,52. Anchor to whitespace/end and pair with exact formatter tests. Do not select the already-selected period then accept an existing matching label: asynchronous reload can replace a details element mid-test. Skip redundant change, or await the exact response/new render.
- Browser live:42views,31/30days,390/1440px, company August/September +Eggbev subtotal contrast,0JSerrors. Full Node48pass. No backend changed; fresh PG dump dual copy/hash + isolated restore and before/after scenario fingerprints passed. Sources/API snapshots identical before/canary/live.
- Independent Python Decimal sum from live facts/cash confirmed daily/monthly company totals within1e-6 USD; September staff−6305.495751869284541554571885 /30 =−210.1831917289761513851523962 daily, displayed−6305.50 and−210.18. The displayed daily roundings need not sum to rounded monthly; do not change monthly totals or create a forced last-day adjustment.

## Release and scope
`deploy/manager-layout-release.py` is a one-shot for this authorization. Three files only: public/operations.css, navigation.css, operations.js. Compatible CSS first, JS last, atomic file replacements; no service/gateway restart, schema, access, credential or financial writes. Rollback only these assets from protected backup, never database rollback by inference. Backup `/home/zeus/mgs-finance-backups/1546719646919434370`; isolated DB `mgs_finance_manager_1546719646919434370`; stage `/var/tmp/mgs-finance-manager-1546719646919434370`; local evidence `apps/finance-system/private/manager-layout-1546719646919434370`.
