# Finance usability, Google Finance GBP, Nicolas access and Jislaine

Authority: Rodolfo1546729477319696405, plus direct mid-turn requests for Nicolas login, critical confirmation **sim**, and Jislaine3000allmonths, in thread1545426987756298340. This supersedes prior sidebar proposal/top-right account, unavailable monthly ROI and August GBP manual-only guidance. No other manager rollout or bot/Discord authorization.

## Active UI
- Black sidebar/white navigation, complete logo centered. Account menu anchored bottom-left, dropdown upward. Nav alone scrolls so account remains reachable. Mobile drawer has44px inside close and clickable dark backdrop; outside topbar toggle is covered by drawer and is NOT a valid sole close control.
- Nicolas: all three cards USD primary + BRL smaller below. FX from same scenario engine `principal|Agosto 2026|F1`; remuneration USD/BRL from existing expense values. Missing FX stays unavailable, never guessed.
- Monthly manager ROI comes directly from engine results at metadata block.row+33 (legacy monthly total row), exposed as `monthly_values`. Do not average daily percentages, derive gross from net, or fill zero when spend absent. Preserve original graph month layout even though visible month can have28/29/30/31days. August Eggbev total source AI53/AJ53 verified, roughly50.40%/25.99% at release; numbers are observations, not baselines.
- Câmbio/Contas/Domínios compact rows; preserve all IDs/names/currencies/bindings and horizontal table scroll. Mobile inputs≥16px, independent bounded tables, forms fit viewport. Native #party select needed explicit width100%/min-width0; generic max-width100% alone allowed381px document on360px.
- History uses Portuguese activity descriptions and before/after fields, no raw JSON/internal event names as primary UI. Mobile cards expose When/Who/What/Reference/Details without horizontal navigation. Pagination/filter retained, allmonths clearly stated, Eastern timestamps.
- **Root cause Invalid Date:** sanitizing PG Date with Object.entries turned Date into{}. `clean` handles Date→ISO before recursion while still redacting credential fields. Never merely hide Invalid Date with a placeholder when timestamps exist in DB. Owner-only access preserved and real manager403verified.
- Commission explanation: belowBRL100000=max(3000,7%profit); at/above100000=10%wholeprofit, no extra floor; inactivezero. Examples50000→3500 and100000→10000 validated. No spreadsheet comment at end. These are existing rules, not a new payroll formula.

## GBP automatic source and exact scope
- **August2026 only** authorized; September2026–December2027 configurations preserved. `ratesFor(period)` grants GBP automatic capability only for2026-08; pass period into every effectiveOverrides caller, including server.mjs mutations and recurring refreshQuotes. Fixed settlement override wins.
- Principal Sheet/I1 NOT edited. Dedicated corporate SharedDrive Google Sheet `1Zk-b8OoaDHrvFtuerf5IuO2jx-phVWXMYi79yqVTZX4`, A1 exact `=GOOGLEFINANCE("GBPUSD")`, minute recalculation. Readback confirmed formula/effective number. Canonical SA/helper only; no Google credential goes to RunCloud/browser.
- Config `apps/finance-system/google-finance-quotes.json`; `sync-quotes.py` collects the two original exact formula quotes and configured GBP source with identity/formula/value guards. Missing config keeps original two-quote route; configured source failure preserves last values/fails closed.
- Existing job685397627b29 stays enabled at26,56 eachhour; wrapper `mgs-finance-quotes.py`→cron-quotes.py→sync-quotes.py. Manual foreground publish validated3quotes and onlyAugustchanged. Cron timing/permissions/gateway unchanged. Hold private/quote-sync.lock during backend/config/quote activation.

## Nicolas real login
- Login `nicolas`, display Nicolas, role manager, manager_key nicolas, enabled following separate criticalconfirmation. No other real user created. Only his manager view, payments and own profile; ten global APIs403, foreign Geizian ledger403, logout401 verified with actual credential.
- Initial password generated/stored by1Password, item **MGS Finance - nicolas - dash.mgsdigitalcorp.com**, vault MGS Conteúdo. User sees item reference, never plaintext password/token on Discord/logs. No forced expiry or first-login reset was implemented; do not claim one. Existing Rodolfo access untouched. App login is NOT authorized-users.json/bot authorization.

## Jislaine salary all registered months
- Exact employee personnel|157, label `Jislaine - conteudo:`, BRL1500→3000 in all17workspaces, August2026–December2027. Preserve id/label/activity/review and other employees. Baseline and source Sheets not mutated; payments remain historical entries, not overwritten by salary changes.
- API `/api/scenarios/{id}/expenses` with fresh period/revision, explicit amount3000/currencyBRL, existing review/date/activity. Stage17months passed; live each-month journal/readback and final PostgreSQL17/17confirmed. Profit/personnel legitimately recalc, other expenses and gross/net/tax/spend preserved.
- Decimal zero representation can change0E-9→0E-12 on recalculation. Compare monetary values semantically, not raw strings; never weaken nonnumeric metadata comparisons. Exact baseline/accounts/profile/source/ledger hashes before/after matched.
- Production17month sequence exceeded tool420s budget after writes. Read back DB before resuming: all17writes were committed,16readbackjournalentries saved. Resume only missing readback + usercreation, never replay writes. Future multi-month jobs should checkpoint and execute in bounded batches (e.g.4months/call), not a single long foreground command. `usability-live-changes.mjs` is one-shot evidence, not a generic repeatable mass worker.

## Acceptance/recovery
- Full52Node/43Python passed; final targeted10Node and actual browser after last mobile fixes passed. PG staged auth/GBP/dates/ROI canary, separate17monthsalary canary. Owner13sections×360/390/768/1440=52checks and28form checks; actual Nicolas3sections×390/1440=6checks/10API denials/logout,0JSerrors.
- Browser must await exact section selectors/data, not merely account-menu mount; shell renders before workspace load. Distinguish bounded horizontal table scroll from document overflow. Persist each check then dedupe/count in code. Initial failures: early DOM assertions; real mobile drawer close obstruction; early rates-row measurement; native party-select width. All corrected and live verified.
- `deploy/usability-release.py`: one-shot eight productionfiles, fresh dualcode/PGbackup, isolatedrestore, stagedreadback; restage phase created fresh prewrite dump/isolatedDB. Dash socket+service stopped together for coherent backend cutover, restarted andverified. Never restart Hermes gateway for this task. Code rollback must preserve new operationaldata; no DB overwrite.
- Evidence `private/usability-1546729477319696405/`; remote backup `/home/zeus/mgs-finance-backups/1546729477319696405`; stage `/var/tmp/mgs-finance-usability-1546729477319696405`; DB `mgs_finance_usability_1546729477319696405` and `_prewrite`. Retained/inventoried, no destructive cleanup.
