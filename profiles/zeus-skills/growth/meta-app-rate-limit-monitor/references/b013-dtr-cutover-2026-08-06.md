# B012 → B013 DTR Cutover — 2026-08-06

> Supersession later on 2026-08-06: all active app assignment reads moved from historical column M / `NO APP` to column N / `APP PROVISORIO`. B013 remains the dedicated DTR app and now selects `APP PROVISORIO = B013` from range `A:N`.

## Decision

Rodolfo confirmed that Meta app B012 was disabled/inactive and replaced it with B013. The Discord channel retained ID `1522830283240505385` and was renamed to `#b013-app-status`.

Current operational set: B001, B002, B003, B004, B005-2, B006-2, B007, B008, B009, B010 and B013. B013 remains a DTR/ChatPion + Meta `debug_token` app and must never enter the generic `/roles` path.

## Credentials

1Password is the only source. The exact current title `BOT B013 Token` has one unique match and is pinned in production by immutable item ID. The separately titled `BOT B013` item is a retired reserve and is non-operational.

The current production record reused the same immutable 1Password item previously pinned by the B012 monitor. This means a title/field change can redirect a still-running predecessor script even when the item ID does not change. Future replacement cutovers must pause the predecessor cron before editing or reusing its pinned item, then validate the app label/app ID before any Sheet reconciliation. The B013 runtime also fails closed unless the credential record still declares `app_name = B013` and the canonical alert channel ID; immutable-ID pinning alone is not an identity guard.

Never print the immutable item ID, app ID, access token, app secret, DTR credentials, page tokens or Discord bot token in chat/reports.

Preflight validation without exposing secrets:

- exact `BOT B013 Token` match count: one;
- required fields present;
- `app_name = B013`;
- alert channel matches `1522830283240505385`;
- Meta app metadata HTTP 200 with app-ID match.

## Runtime

```text
Script    /root/.hermes/profiles/zeus/scripts/b013-dtr-link-watch.sh
State     /root/mgs-agent/data/b013-dtr-link-monitor-state.json
Cron      498fb0d95e10 / b013-dtr-link-watch
Schedule  2-59/9 0,8-23 * * * ET
Lock      /var/lock/b013-dtr-link-watch.lock
Channel   1522830283240505385 / #b013-app-status
Config    unique current BOT B013 Token item pinned by immutable ID
Sheet     every current row where APP PROVISORIO = B013
```

The preserved predecessor `/root/.hermes/profiles/zeus/scripts/b012-dtr-link-watch.sh` is a fail-closed rollback artifact and exits immediately. The B012 state remains preserved for audit/rollback and is not a B013 baseline.

`meta-app-roles-watch.sh` excludes `BOT B011 Token`, `BOT B012 Token` and `BOT B013 Token`; `ROLE_RECONCILIATION_EXCLUDED_APPS` contains B011, B012 and B013. B013 Sheet X markers are owned only by the dedicated DTR monitor.

## Cutover incident and reconciliation

The B013 credential update reached the reused immutable item before the B012 cron was paused. At 18:34 ET, the predecessor monitor evaluated 35 B012 targets using B013 credentials, posted a false B012 disconnection family in the renamed B013 channel and wrote 29 X updates while one unknown row was preserved. Zeus paused the cron, removed the six false Discord messages, preserved the predecessor state/backup and cut over the job.

By the B013 production baseline, the same operational rows had been reassigned to `NO APP = B013`; the B013 monitor found their X markers already consistent with the live B013 result, so no blind rollback of Sheet markers was performed.

## First B013 production validation

- Script shell and embedded-Python syntax passed.
- B013 dry run completed with real DTR/Meta/Sheet reads.
- First live baseline exited 0 with empty stdout and wrote a separate B013 state.
- Live state: 35 targets, 0 linked, 34 confirmed unlinked and 1 inconclusive; zero DTR/Graph-visible pages.
- Sheet readback: 34 checked/marked rows, one unknown preserved, no pending write delta on the baseline run.
- Forced live alert produced one canonical B013 app-alert family in `#b013-app-status`: one native embed titled `Meta APP - B013` plus three code-block messages, with no duplicate B012 family.
- The persistent unknown branch separately emitted the expected infra diagnostic because the inconclusive row reached its threshold.
- Cron was renamed in place to `b013-dtr-link-watch`, pointed to the B013 script, resumed on the existing cadence and remained `deliver=local`/`no_agent`.
- The resumed 18:56 ET cycle detected the first confirmed B013 link and posted one real B013 transition family at 19:01 ET. The next scheduled cycle started at 19:02 ET and completed status `ok` at 19:07 ET after the identity guard patch: 35 targets, 1 linked, 33 confirmed unlinked, 1 inconclusive, zero Sheet write delta and `alerts_sent=0`.

## Current operational interpretation

Live Graph readback on 2026-08-06 confirmed the B013 app credential is healthy: app metadata HTTP 200 with app-ID match. `/roles` returned exactly two administrator profiles:

- `Sem Taimenas` — primary app admin/owner used by Rodolfo;
- `Fabiana Da Silva Maciel Da Silva Maciel` — backup administrator/segurador retained so Rodolfo can recover the app or BM when the incident is light/reversible.

The later controlled force-live DTR validation selected 34 current `APP PROVISORIO = B013` targets and converged to 29 linked, 5 confirmed unlinked, 0 unknown. App capability was healthy: 29 valid `debug_token` results, 327 pages visible in DTR and 325 via Graph, with no linked account at zero Graph pages. Therefore, distinguish the healthy B013 app credential/roles from individual DTR profile links: the app token is valid, while five current profile links still do not validate against B013 and remain marked fail-closed in the Sheet.

## Restriction classification correction — 2026-08-21

Rodolfo provided direct Meta for Developers evidence with the `Restritos (1)` filter selected and B013-2 listed with `Ações necessárias (1)` / `Resolve`. This is authoritative dashboard evidence that B013-2 was restricted.

For this incident, the yellow `B013-2 — validação inconclusiva persistente` alert was the downstream technical manifestation of that restriction: all monitored accounts lost conclusive `debug_token`/Graph validation and the monitor preserved the Sheet fail-closed. Do not report this alert as an unrelated generic validation failure once the Meta dashboard confirms the app is restricted.

The separate red `B013-2 — Messenger bloqueado por permissões/BM` alert is only the automatic DTR-versus-Graph capability-collapse branch. Its evidence floor requires at least three linked accounts with valid `debug_token`, successful `/me/accounts`, DTR-visible pages and zero Graph pages. A hard restriction can destroy that evidence floor and leave the automatic classifier at `inconclusive`; absence of the red alert therefore does not negate direct Meta dashboard restriction evidence.

Operational rule: when direct Meta for Developers evidence shows the app under `Restritos`, classify the incident as **app restriction**. Treat the yellow persistent-inconclusive alert as the fail-closed monitoring symptom and preserve the Sheet until recovery is validated.
