# B012 → B013 DTR Cutover — 2026-08-06

## Decision

Rodolfo confirmed that Meta app B012 was disabled/inactive and replaced it with B013. The Discord channel retained ID `1522830283240505385` and was renamed to `#b013-app-status`.

Current operational set: B001, B002, B003, B004, B005-2, B006-2, B007, B008, B009, B010 and B013. B013 remains a DTR/ChatPion + Meta `debug_token` app and must never enter the generic `/roles` path.

## Credentials

1Password is the only source. The exact current title `BOT B013 Token` has one unique match and is pinned in production by immutable item ID. The separately titled `BOT B013` item is a retired reserve and is non-operational.

The current production record reused the same immutable 1Password item previously pinned by the B012 monitor. This means a title/field change can redirect a still-running predecessor script even when the item ID does not change. Future replacement cutovers must pause the predecessor cron before editing or reusing its pinned item, then validate the app label/app ID before any Sheet reconciliation.

Never print the immutable item ID, app ID, access token, app secret, DTR credentials, page tokens or Discord bot token in chat/reports.

Preflight validation without exposing secrets:

- exact `BOT B013 Token` match count: one;
- required fields present;
- `app_name = B013`;
- alert channel matches `1522830283240505385`;
- Meta app metadata HTTP 200 with app-ID match.

## Runtime

```text
Script    /root/.hermes/profiles/zeus/scripts/b013-dtr-link-watch.sh
State     /root/mgs-agent/data/b013-dtr-link-monitor-state.json
Cron      498fb0d95e10 / b013-dtr-link-watch
Schedule  2-59/9 0,8-23 * * * ET
Lock      /var/lock/b013-dtr-link-watch.lock
Channel   1522830283240505385 / #b013-app-status
Config    unique current BOT B013 Token item pinned by immutable ID
Sheet     every current row where NO APP = B013
```

The preserved predecessor `/root/.hermes/profiles/zeus/scripts/b012-dtr-link-watch.sh` is a fail-closed rollback artifact and exits immediately. The B012 state remains preserved for audit/rollback and is not a B013 baseline.

`meta-app-roles-watch.sh` excludes `BOT B011 Token`, `BOT B012 Token` and `BOT B013 Token`; `ROLE_RECONCILIATION_EXCLUDED_APPS` contains B011, B012 and B013. B013 Sheet X markers are owned only by the dedicated DTR monitor.

## Cutover incident and reconciliation

The B013 credential update reached the reused immutable item before the B012 cron was paused. At 18:34 ET, the predecessor monitor evaluated 35 B012 targets using B013 credentials, posted a false B012 disconnection family in the renamed B013 channel and wrote 29 X updates while one unknown row was preserved. Zeus paused the cron, removed the six false Discord messages, preserved the predecessor state/backup and cut over the job.

By the B013 production baseline, the same operational rows had been reassigned to `NO APP = B013`; the B013 monitor found their X markers already consistent with the live B013 result, so no blind rollback of Sheet markers was performed.

## First B013 production validation

- Script shell and embedded-Python syntax passed.
- B013 dry run completed with real DTR/Meta/Sheet reads.
- First live baseline exited 0 with empty stdout and wrote a separate B013 state.
- Live state: 35 targets, 0 linked, 34 confirmed unlinked and 1 inconclusive; zero DTR/Graph-visible pages.
- Sheet readback: 34 checked/marked rows, one unknown preserved, no pending write delta on the baseline run.
- Forced live alert produced one canonical B013 app-alert family in `#b013-app-status`: one native embed titled `Meta APP - B013` plus three code-block messages, with no duplicate B012 family.
- The persistent unknown branch separately emitted the expected infra diagnostic because the inconclusive row reached its threshold.
- Cron was renamed in place to `b013-dtr-link-watch`, pointed to the B013 script, resumed on the existing cadence and remained `deliver=local`/`no_agent`.

## Operational interpretation

The B013 app credential is healthy at app-metadata level, but the current DTR/ChatPion profile connections are not migrated yet: zero current B013 targets are confirmed linked. The monitor must keep the disconnected X markers and alert current state until each profile is reconnected and a valid B013 `debug_token`/page capability is observed. Unknown rows remain fail-closed and must never be marked or cleared from inconclusive evidence.
