# B011 → B012 DTR Cutover — 2026-07-30

## Decision

Rodolfo retired B011 and activated B012 as the DTR/ChatPion app. The Discord channel was renamed to `#b012-app-status` and retained ID `1522830283240505385`.

## Credential selection

The cutover initially had two 1Password items with the exact title `BOT B012 Token`, so title-based resolution selected the retired reserve item and was unsafe. Rodolfo then renamed the old reserve item to `BOT B012`. Live 1Password readback now shows exactly two distinct titles — `BOT B012 Token` (current) and `BOT B012` (retired reserve) — with exactly one current-title match, zero duplicate-title collisions and resolver ID match to the pinned production item.

Production requirements:

- Keep the current validated B012 item pinned by immutable 1Password item ID in `b012-dtr-link-watch.sh`.
- `mgs-op-item-resolver.py` must continue to fail closed on any future duplicate exact title.
- Never print `app_id`, `access_token`, `app_secret`, page tokens or DTR credentials.
- The retired `BOT B012` reserve item remains non-operational; do not delete it without Rodolfo's explicit credential-change authorization.

The current item was validated without exposing secrets: app metadata HTTP 200 with ID match, user token `/me` HTTP 200, `debug_token` HTTP 200 with `is_valid=true` and app-ID match, and `/me/accounts` HTTP 200.

## Runtime

```text
Script  /root/.hermes/profiles/zeus/scripts/b012-dtr-link-watch.sh
State   /root/mgs-agent/data/b012-dtr-link-monitor-state.json
Cron    498fb0d95e10 / b012-dtr-link-watch
Schedule 24 8-23 * * * ET
Channel 1522830283240505385 / #b012-app-status
Config  current BOT B012 Token item pinned by immutable ID
```

`meta-app-roles-watch.sh` must exclude both `BOT B011 Token` and `BOT B012 Token` from `/roles` discovery and keep both B011/B012 in `ROLE_RECONCILIATION_EXCLUDED_APPS`. B012 Sheet X markers are owned exclusively by the DTR monitor.

## Sheet contract

- Read and write exclusively through the `mgs-core-prod` Service Account helper; public CSV is retired.
- Default target set is every row where `NO APP = B012`, including rows already marked `X`.
- `Migracao` is informational unless Rodolfo explicitly requests a filtered audit.
- `debug_token.app_id == B012 app_id && is_valid == true` means linked even when `/me/accounts` returns zero pages.
- Write `X` only for `unlinked_confirmed`; preserve unknown rows fail-closed; clear `X` automatically after a valid B012 reconnection.

## Cutover finding

The 2026-07-30 initial full dry/live run found 28 B012 Sheet targets: 0 linked to B012, 27 confirmed unlinked and 1 inconclusive because the profile was absent from the DTR account switcher. This proved the new app credential was healthy but the per-profile DTR OAuth connections had not yet been migrated.

After Rodolfo reconnected most users, the 16:07 ET full live run found 23/28 linked, 5 confirmed unlinked and zero inconclusive. Account capability was healthy: 23 valid `debug_token` app matches, 23 `/me/accounts` HTTP 200, 275 DTR-visible pages, 273 Graph-visible pages and 269 pages confirmed subscribed to B012. The Service Account Sheet sync cleared 23 `X` markers and retained `X` only for Kaio Sousa, Erna Ansary, Ademar Soares Nunes, Barbara Cristina and Hùng Hợp Tiến. A real change alert was read back in `#b012-app-status`.

A force-live request during a material-change run originally emitted both the automatic change alert and a duplicate force snapshot. The duplicate snapshot messages were deleted, the accurate change alert was preserved, and the runtime now suppresses the force snapshot whenever the same run already sent the link-change alert.

At Rodolfo's request, the B012 native summary card was standardized to the accepted B010 visual model: `Meta APP - B012`, concise live-data description and inline `ESTADO / CONTAGEM / PENDENTES / PÁGINAS / DTR / META` fields. Forced runs must say `Alerta live solicitado` even when the same run detects a real transition, must not replay cached additions, and omit the inconclusive section when empty. The 16:15 ET forced run completed at 16:25 ET and found a new real Yudi Anggara disconnect, resulting in 22/28 linked, 6 confirmed unlinked, zero inconclusive and one new Sheet `X`. Its cleaned three-message alert was read back as messages `1532484386061160632`, `1532484387185361196` and `1532484388166570158`. The scheduled 16:24 cron remained enabled; it encountered the same non-blocking lock while the authorized manual run was still active and the next normal cycle stayed scheduled for 17:24 ET.

The retired B011 cron run at 14:24 ET evaluated the now-retired app and wrote false X markers during the transition. The B011 cron was paused before cutover; the 15:04 generic Meta `/roles` run was interrupted and B012 was added to its DTR-only exclusion guard.
